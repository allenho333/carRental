setup local database mysql server, login as root with passsword abcd=1234, port 3306, and create a new databse called carrental
database url is :'mysql+pymysql://root:abcd=1234@localhost:3306/carrental'
to create the database, run the database.shell.sh in folder
run pip install -r requirements.txt to install all packages
run python main.py to run the applcation 
